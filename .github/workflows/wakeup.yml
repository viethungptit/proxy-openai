name: Wake up service

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call service
        run: |
          curl -L https://proxy-openai-1bm5.onrender.com/docs
